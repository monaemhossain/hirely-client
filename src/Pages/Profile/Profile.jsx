import { useEffect } from "react";

const Profile = () => {
    useEffect(() => {
        document.title = 'Hirely | Profile';
    }, []);
    return (
        <div>
            
        </div>
    );
};

export default Profile;